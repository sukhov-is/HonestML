# Операционные аспекты (Day-2) — <Feature>

> Как фича живёт у пользователя и меняется без поломок. Руководство — `operational.md` в references скила `architecture-design`.
> Значимые решения здесь дублируй как ADR (со ссылкой отсюда).

## Эволюция сериализуемых контрактов
- **`ModelArtifact` (`ARTIFACT_VERSION`):** <меняется или нет; аддитивная дельта
  против бампа версии; что происходит при чтении несовместимой версии>
- **Манифест прогона / ключ кэша:** <новые поля, входят ли в run-fingerprint и
  почему> — ADR: <…>
- **Публичный API и дефолты конфига:** <что видит пользователь, который ничего не
  менял; обратная совместимость>

## Релиз и откат
- **Релиз:** <semver-эффект, строка CHANGELOG, за каким значением конфига фича>
- **Аддитивность:** <кого фича не затрагивает и почему>
- **План отката:** <откат кода; читаемость артефактов прежней версии>
- **Опциональные зависимости:** <нужен ли extra, где падает `MissingDependencyError`,
  под каким `importorskip` стоит тест>

## Воспроизводимость и пересчёт
- **Детерминизм:** <что фиксирует сид; где разрешаются ничьи>
- **Кэш:** <меняется ли fingerprint; почему устаревшее попадание невозможно>
- **Пересчёт:** <нужен ли новый baseline бенчмарка и что это значит для чисел>

## Бюджеты и границы ошибок
- **Бюджет прогона:** <поведение при исчерпании; как деградация видна в исходе>
- **Fail-fast:** <какие мисконфиги отсекаются на границе и какой ошибкой>
- **Стратегия ошибок:** <где падаем громко, где деградируем; изоляция сбоя кандидата>
- **Граница доверия:** <десериализация артефакта/кэша, требования к источнику>

## Диагностика прогона
- **Манифест:** <какие разрешённые параметры записываются>
- **Отчёт:** <что видно в лидерборде, полосе честности, исходе бюджета>
- **Предупреждения:** <на какие ситуации риска; что в логи не попадает>
